name: Publish Website

on:
  push:
    branches:
      - main


jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get Latest Code
      uses: actions/checkout@v3
    
    - name: 📂 Sync Files
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: ssh.cvw2wabs0.service.one
        remote-user: cvw2wabs0_ssh
        private-ssh-key: ${{ secrets.SSH_KEY }}
        destination-path: /webroots/7a4ab272/wp-content/themes/foodza